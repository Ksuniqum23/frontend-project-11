1. Установила сонар (зарегалась, закинула секрет в гит, добавила бандл)
2. Добавила проверку линтера

sudo apt update     //- обновление пакетов системы

sudo apt install nodejs npm -y  //платформа Node.js, нужна для запуска JS-кода
                                //Node Package Manager, используется для установки зависимостей (библиотек)

npm init -y     // инициализация проекта. Создаёт package.json с настройками проекта по умолчанию.

npm install eslint webpack webpack-cli webpack-dev-server --save-dev
    // eslint → линтер для проверки кода.
       webpack → сборщик кода.
       webpack-cli → интерфейс командной строки для Webpack.
       webpack-dev-server → локальный сервер для разработки.
       --save-dev → сохраняем зависимости только для разработки, не для продакшена.

3. add Makefile

sudo apt install make -y
    //чтоб работали команды make в workflows lint

git rm -r --cached node_modules
    //убираем из индекса нод модули

4. Добавила eslint.config
    npm install --save-dev \
    npm install --save-dev eslint @eslint/eslintrc @eslint/js eslint-plugin-import globals eslint-config-airbnb-base
    npm install --save-dev eslint @eslint/eslintrc @eslint/js eslint-plugin-import globals eslint-config-airbnb-base --legacy-peer-deps

     npx eslint .
     npx eslint . --fix

5. VITE
    npm create vite@latest
    npm install --save-dev vite

    curl -fsSL https://deb.nodesource.com/setup_24.x | sudo -E bash -
    sudo apt-get install -y nodejs
    node -v - 24!!!

    npm run dev
    ЗАПУСТИЛСЯ!!

6. Bootstrap
    npm install bootstrap @popperjs/core

npm install yup
npm install axios
npm install i18next
npm install @stylistic/eslint-plugin

